# Migration `20201023122504-initial-migration`

This migration has been generated by Zane Helton at 10/23/2020, 8:25:04 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"email" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Token" (
"id" text   NOT NULL ,
"contract" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Watcher" (
"id" text   NOT NULL ,
"isActive" boolean   NOT NULL DEFAULT true,
"fromAmount" Decimal(65,30)   NOT NULL ,
"toAmount" Decimal(65,30)   NOT NULL ,
"fromTokenId" text   NOT NULL ,
"toTokenId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

CREATE UNIQUE INDEX "Token.contract_unique" ON "public"."Token"("contract")

ALTER TABLE "public"."Watcher" ADD FOREIGN KEY ("fromTokenId")REFERENCES "public"."Token"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Watcher" ADD FOREIGN KEY ("toTokenId")REFERENCES "public"."Token"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201023122504-initial-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+datasource db {
+    provider = "postgresql"
+    url = "***"
+}
+
+generator client {
+    provider = "prisma-client-js"
+}
+
+model User {
+    id    String @id @default(uuid())
+    email String @unique
+}
+
+model Token {
+    id       String @id @default(uuid())
+    contract String @unique
+
+    aWatch Watcher[] @relation("fromToken")
+    bWatch Watcher[] @relation("toToken")
+}
+
+model Watcher {
+    id         String  @id @default(uuid())
+    isActive   Boolean @default(true)
+    fromAmount Float
+    toAmount   Float
+
+    fromToken Token @relation(fields: [fromTokenId], references: [id], "fromToken")
+    toToken   Token @relation(fields: [toTokenId], references: [id], "toToken")
+
+    fromTokenId String
+    toTokenId   String
+}
```


